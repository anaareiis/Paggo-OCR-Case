version: "3.8"
services:
  db:
    image: postgres:15
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: paggo
      POSTGRES_PASSWORD: paggo
      POSTGRES_DB: paggo
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
